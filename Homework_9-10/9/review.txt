T1

1. Вместо !(0 < radius || radius < 0) используйте проверку типов.
2. (radius == 0 || radius === null || radius === ' ') можно сократить до !radius.

T2. Те же замечания.

T3.

1. Методы ничего выводят в консоль, а возвращают значения (return).
2. Pow не работает с неположительной и неотрицательной степенью.
3. С подсчётом числа PI что-то не так. Переменная в цикле не инициализируется,
	и на первой итерации происходит деление на ноль. Попробуйте ряд Лейбница.
4. min, max принимают ряд аргументов-чисел, а не один массив.